version: '3'
services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./html:/usr/share/nginx/html:ro

  php-fpm:
    image: php:7.4-fpm
    volumes:
      - ./html:/usr/share/nginx/html
