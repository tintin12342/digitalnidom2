version: '3.8'

services:
  nginx:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html
      - ./default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php

  php:
    image: php:8.0-fpm
    volumes:
      - .:/var/www/html
